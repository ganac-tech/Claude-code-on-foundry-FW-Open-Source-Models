# secrets — never commit
.env

# Local working notes — not published.
evaluation/local/FOUNDRY-NOTES.md

# 288MB binary — install per README instead
bin/

# generated at gateway start (embeds the host CA bundle)
.aigw-foundry.generated.yaml

# local run artifacts
.venv/
evaluation/local/*.results.jsonl
test/upstream-requests.jsonl
archive.zip
__pycache__/
*.pyc
